package tterrag.core.api.common.imc;

public class IMC
{
    /**
     * Use an IMC event with this string to add a right click crop as compatible. Syntax is a single
     * string, with '|' separating the values, in the order seed, block, meta. A valid entry would
     * look like this:
     * <p>
     * {@literal "minecraft:wheat_seeds|minecraft:wheat|7"}
     */
    public static final String ADD_RIGHT_CLICK_CROP = "addRightClickCrop";
}
